# Roadmap

Java Game Launcher 的定位是：为 Mindustry 提供一等启动体验，同时支持少量“发行形式清晰、启动流程可控”的可执行 Java 游戏。Minecraft 版本资源、认证、资产和依赖解析明确不在范围内，它更适合专用启动器。

## 已完成

- 便携 Java/JAR 自动发现、class 版本和原生架构检查；
- 自动/保守/均衡/性能 JVM 预设及物理内存检测；
- Mindustry 数据目录、完整 JRE 模块预检和 Flatpak 图形桥接；
- 实时日志、持久日志和常见启动失败智能诊断；
- 数据安全备份/恢复底层服务；
- Mindustry 模组扫描、安全启停和批量回滚；
- 无版本号的精简多实例配置、稳定实例 ID 和共享数据目录警告；
- Mindustry Server 实时控制台、安全退出与强制停止；
- 一次性无模组安全启动及崩溃后持久恢复；
- 数据恢复 TUI：安全预览、二次确认和恢复前自动保护备份；
- 启动前完整检查页及 CLI `--preflight` JVM 参数试运行；
- 按实例浏览、诊断和删除旧启动日志；
- 用户明确触发、SHA-256 校验的 Azul Zulu 最新 LTS JRE 安装；
- Mindustry 桌面启动失败后显式发起的一次性无模组安全重试；
- Windows、Linux、macOS 的 amd64/arm64 CI 构建。

## 下一阶段

1. 启动诊断包：显式导出脱敏后的环境、配置和所选日志，便于反馈无法复现的启动问题。
2. Mindustry 体验：轻量提示重复模组名、损坏压缩包或元数据异常；不做依赖解析、不联网，也不阻止启动。
3. 备份生命周期：保留数量/空间上限、删除确认和恢复内容过滤。
4. 适配器验证：只为发行形式清晰、单一可执行 JAR、启动过程可控且有实际用户需求的游戏增加小型预检，不引入通用依赖解析器。

服务器重启、常用命令历史、定时任务和日常运维明确不在启动器职责内；Java 和游戏也不会自动下载或更新。

## 适配器候选

- Unciv、Shattered Pixel Dungeon Desktop：仅在其官方桌面发行仍提供单一可执行 JAR 且通用模式确有缺口时考虑；
- RuneLite 自举、Slay the Spire/ModTheSpire classpath、Minecraft、Project Zomboid 等复杂发行系统不在近期范围内，它们更适合专用启动器。
